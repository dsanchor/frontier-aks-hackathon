# Coach YAML Solutions

Ready-to-apply manifests coaches can share selectively or use to unblock teams.
Do **not** distribute these at the start — let teams attempt each challenge first.

| File | Challenge | Description |
|------|-----------|-------------|
| `gateway.yaml` | 03 — Option A | `Gateway` + `HTTPRoute` via Gateway API (App Routing add-on) |
| `agc.yaml` | 03 — Option B | `ApplicationLoadBalancer` + `HTTPRoute` for AGC |
| `fabtech-helmrelease.yaml` | 07 | Flux `HelmRelease` for GitOps |
